---
# yaml-language-server: $schema=https://schema.blue-build.org/module-stage-list-v1.json
modules:
  # 1Password from official repository
  # Includes both GUI and op CLI
  - type: bling
    install:
      - 1password
